# Privacy-Preserving Lightweight RAG System

## 面向隐私保护的轻量级RAG系统设计与开发

本毕业设计项目实现了一个面向本地化部署和隐私敏感场景的轻量级检索增强生成（RAG）系统。系统在保证问答准确性的前提下，集成端到端的数据加密机制以保护私有知识库的安全，同时通过采用轻量级深度学习模型和量化技术优化系统的推理延迟和资源占用。

### 🌟 核心特性

#### 1. 隐私保护机制
- **端到端加密**：使用AES-256-GCM算法对文档内容进行加密
- **本地化部署**：所有数据和处理都在本地完成，杜绝数据外泄
- **密文存储**：向量数据库仅存储加密后的文本，原文永不明文存储
- **安全解密**：仅在查询时临时解密相关片段，用后即弃

#### 2. 轻量级设计
- **小参数量模型**：使用 all-MiniLM-L6-v2（384维）作为嵌入模型
- **量化支持**：支持 4-bit 量化的 LLM（如 Llama 3.2 3B）
- **资源优化**：适用于CPU环境，降低硬件要求
- **高效检索**：基于向量相似度的快速检索

#### 3. 完整的RAG流程
- **文档处理**：支持 PDF、TXT、DOCX 格式
- **智能分块**：递归分块策略，保持语义完整性
- **向量检索**：基于 Qdrant 的高效向量搜索
- **上下文生成**：结合检索结果生成准确答案

#### 4. 审计与合规
- **操作日志**：记录所有系统操作（不含敏感数据）
- **完整性校验**：使用哈希验证日志完整性
- **可追溯性**：支持审计追踪，满足合规要求

### 📁 项目结构

```
graduation-design/
├── config/                      # 配置文件
│   ├── __init__.py             # 配置管理模块
│   └── config.yaml             # 系统配置
├── src/                        # 源代码
│   ├── document_processor/     # 文档处理模块
│   │   ├── parser.py          # 文档解析器
│   │   └── chunker.py         # 文本分块器
│   ├── encryption/            # 加密模块
│   │   └── aes_encryption.py # AES加密实现
│   ├── embedding/             # 嵌入模块
│   │   └── embedding_model.py # 嵌入模型封装
│   ├── vector_db/             # 向量数据库
│   │   └── qdrant_db.py      # Qdrant集成
│   ├── llm/                   # LLM模块
│   │   └── ollama_client.py  # Ollama客户端
│   ├── audit/                 # 审计模块
│   │   └── audit_logger.py   # 审计日志
│   ├── rag_pipeline/          # RAG流程
│   │   └── rag_system.py     # 完整RAG系统
│   └── web/                   # Web界面
│       └── app.py             # Streamlit应用
├── tests/                      # 测试文件
├── data/                       # 数据目录
│   ├── documents/             # 原始文档
│   ├── encrypted/             # 加密数据
│   └── vectors/               # 向量数据
├── logs/                       # 日志目录
├── requirements.txt            # Python依赖
└── README.md                   # 项目说明
```

### 🚀 快速开始

#### 前置要求

1. **Python 3.8+**
2. **Qdrant** 向量数据库
3. **Ollama** 本地 LLM 服务

#### 安装步骤

##### 1. 克隆项目
```bash
git clone https://github.com/Irene-hua/graduation-design.git
cd graduation-design
```

##### 2. 安装Python依赖
```bash
pip install -r requirements.txt
```

##### 3. 安装并启动 Qdrant
```bash
# 使用 Docker
docker run -p 6333:6333 -p 6334:6334 \
    -v $(pwd)/qdrant_storage:/qdrant/storage \
    qdrant/qdrant

# 或使用 docker-compose（推荐）
cat > docker-compose.yml << EOF
version: '3.8'
services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage
EOF

docker-compose up -d
```

##### 4. 安装并启动 Ollama
```bash
# 访问 https://ollama.ai 下载安装

# 拉取轻量级模型
ollama pull llama3.2:3b

# 启动服务（通常自动启动）
ollama serve
```

##### 5. 配置系统
编辑 `config/config.yaml` 根据需要调整配置参数。

#### 运行系统

##### 启动Web界面
```bash
streamlit run src/web/app.py
```

访问 http://localhost:8501 使用系统。

##### 使用Python API
```python
from pathlib import Path
from src.rag_pipeline import RAGSystem
from src.encryption import generate_key, save_key

# 生成加密密钥
key = generate_key()
save_key(key, "config/encryption.key")

# 初始化系统
rag_system = RAGSystem(
    encryption_key=key,
    embedding_model_name="sentence-transformers/all-MiniLM-L6-v2",
    llm_model_name="llama3.2:3b"
)

# 导入文档
rag_system.ingest_document("path/to/document.pdf")

# 查询
result = rag_system.query("你的问题?")
print(result['answer'])
```

### 🔧 系统架构

#### 数据流程

```
┌─────────────┐
│  用户上传   │
│   文档      │
└──────┬──────┘
       │
       ▼
┌──────────────┐
│  文档解析    │  (PDF/TXT/DOCX)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  文本分块    │  (智能切块)
└──────┬───────┘
       │
       ├─────────────┬──────────────┐
       ▼             ▼              ▼
┌──────────┐  ┌──────────┐  ┌──────────┐
│ 生成向量 │  │ AES加密  │  │ 元数据   │
└─────┬────┘  └─────┬────┘  └─────┬────┘
      │             │              │
      └─────────────┴──────────────┘
                    │
                    ▼
         ┌──────────────────┐
         │  Qdrant 向量库   │
         │  (向量 + 密文)    │
         └──────────────────┘
```

```
┌─────────────┐
│  用户查询   │
└──────┬──────┘
       │
       ▼
┌──────────────┐
│  查询向量化  │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  向量检索    │  (Top-K 相似度)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  密文解密    │  (临时解密)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  上下文构建  │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  LLM 生成    │  (本地 Ollama)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  返回答案    │
└──────────────┘
```

#### 核心组件

1. **文档处理层**
   - `DocumentParser`: 多格式文档解析
   - `TextChunker`: 智能文本分块

2. **加密层**
   - `AESEncryption`: AES-256-GCM 加密/解密

3. **嵌入层**
   - `EmbeddingModel`: 轻量级嵌入模型封装

4. **存储层**
   - `QdrantDB`: 向量数据库操作

5. **生成层**
   - `OllamaClient`: 本地 LLM 接口

6. **编排层**
   - `RAGSystem`: 端到端 RAG 流程

7. **审计层**
   - `AuditLogger`: 操作日志与完整性验证

### 🔒 隐私保护分析

#### 数据静态安全
- ✅ 所有文档内容以AES-256-GCM密文形式存储
- ✅ 向量数据库不包含任何明文信息
- ✅ 加密密钥单独管理，设置严格权限

#### 数据动态安全
- ✅ 解密仅在内存中进行，用后立即释放
- ✅ 不生成临时明文文件
- ✅ 所有处理在本地完成，无网络传输

#### 操作可审计
- ✅ 记录所有查询、检索、生成操作
- ✅ 日志不包含敏感内容（仅记录哈希）
- ✅ 支持完整性校验，防篡改

#### 架构优势对比

| 特性 | 本系统（本地部署） | 云端RAG |
|------|------------------|---------|
| 数据位置 | 本地存储，完全控制 | 上传到云端 |
| 隐私风险 | 极低（无外泄风险） | 中-高（取决于服务商） |
| 网络依赖 | 无（离线可用） | 强（必须联网） |
| 数据主权 | 完全自主 | 受服务商约束 |
| 合规性 | 易满足严格要求 | 需评估服务商资质 |

### 📊 性能评估

#### 测试指标

1. **准确性指标**
   - F1 Score
   - Precision（精确率）
   - Recall（召回率）

2. **效率指标**
   - 响应时间（Response Time）
   - 生成延迟（Generation Latency）
   - 内存占用（Memory Usage）

3. **轻量化验证**
   - 量化前后对比
   - CPU vs GPU 性能
   - 不同模型大小对比

#### 运行测试

```bash
# 单元测试
pytest tests/ -v

# 性能测试
python tests/benchmark.py
```

### 🎯 技术选型说明

#### 嵌入模型：all-MiniLM-L6-v2
- **参数量**：22M（轻量级）
- **维度**：384
- **性能**：在 CPU 上快速推理
- **质量**：在多个基准测试中表现优秀

#### LLM：Llama 3.2 3B
- **参数量**：3B（适合本地部署）
- **量化**：支持 4-bit 量化
- **内存**：量化后约 2-3GB
- **推理**：CPU 可用，GPU 更佳

#### 向量数据库：Qdrant
- **性能**：高效的向量检索
- **易用**：简单的 API 和部署
- **本地**：支持完全本地部署
- **扩展**：支持大规模向量存储

### 📝 配置说明

#### config/config.yaml 主要参数

```yaml
embedding:
  model_name: "sentence-transformers/all-MiniLM-L6-v2"  # 嵌入模型
  dimension: 384                                        # 向量维度
  
llm:
  model_name: "llama3.2:3b"    # LLM 模型
  quantization: "4bit"          # 量化配置
  temperature: 0.7              # 生成温度
  
vector_db:
  top_k: 5                      # 检索数量
  
document_processing:
  chunk_size: 512               # 分块大小
  chunk_overlap: 50             # 分块重叠
  
encryption:
  algorithm: "AES"              # 加密算法
  key_size: 256                 # 密钥长度
```

### 🐛 常见问题

#### 1. Qdrant 连接失败
**原因**：Qdrant 服务未启动
**解决**：
```bash
docker-compose up -d
```

#### 2. Ollama 模型未找到
**原因**：模型未下载
**解决**：
```bash
ollama pull llama3.2:3b
```

#### 3. 内存不足
**原因**：模型太大或数据量太大
**解决**：
- 使用更小的模型
- 启用量化
- 减少 batch_size
- 降低 top_k 值

#### 4. 推理速度慢
**解决**：
- 使用 GPU（如果可用）
- 启用 4-bit 量化
- 减少 max_tokens
- 使用更小的模型

### 📚 参考文献

1. **MiniRAG: Towards Extremely Simple Retrieval-Augmented Generation**
   - 提供核心 RAG 算法思想

2. **The Good and The Bad: Exploring Privacy Issues in RAG**
   - 明确隐私风险与需求

3. **Privacy-Preserving Federated Embedding Learning for Localized RAG**
   - 提供隐私保护的系统设计视角

### 👥 贡献指南

欢迎提交 Issue 和 Pull Request！

### 📄 开源协议

MIT License

### 📞 联系方式

如有问题或建议，请提交 Issue 或联系项目维护者。

---

**毕业设计项目** - 面向隐私保护的轻量级RAG系统设计与开发

© 2024 - 保留所有权利
